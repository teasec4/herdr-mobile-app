# собранные бинари релея (корень + плагин)
/relay
/plugin/bin/
/cmd/relay/relay

# macOS
.DS_Store

# Go
*.test
*.out
coverage.*
.gocache/

# Flutter/Dart (в client/ есть свой .gitignore, дублируем на всякий случай)
client/.dart_tool/
client/build/
client/.flutter-plugins*
client/.idea/
client/*.iml

# логи релея/плагина
herdrelay*.log

# Claude Code: локальные настройки (не для git)
.claude/settings.local.json
